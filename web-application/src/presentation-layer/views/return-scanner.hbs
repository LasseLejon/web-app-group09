<h1>Return Scanner</h1>

{{#if errors.length}}
    <ul>
        {{#each errors}}
            <li>{{this}}</li>
        {{/each}}
    </ul>
{{/if}}

{{#if scanner}}
    {{#if scanner.scannerInUse}}
        <form action="/scanner/return/{{scanner.scannerId}}" method="post">
            <p>Are you sure you wan't to return scanner {{scanner.scannerNumber}}</p>
            <div>
                <button type="submit">Return</button>
            </div>
        </form>
    {{else}}
        <p>This scanner is not in use</p>
    {{/if}}
{{else}}
    <p>That specific scanner does not exist</p>
{{/if}}